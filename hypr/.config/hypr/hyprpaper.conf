# Splash text rendering
splash = false
# Disabling IPC prevents from using hyprctl hyprpaper to change wallpapers dynamically on the fly
ipc = off

wallpaper {
  monitor = HDMI-A-1
  path = $HOME/.config/hypr/wallpapers/a_motorcycle_parked_outside_a_restaurant.jpg
  fit_mode = cover
}

